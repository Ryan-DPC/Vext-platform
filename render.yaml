services:
  # --------------------------------------------------------------------------------
  # BACKEND API (REST)
  # --------------------------------------------------------------------------------
  - type: web
    name: vext-backend
    region: frankfurt
    env: node
    plan: free # Change to starter/standard for production
    rootDir: . # Root context for Monorepo
    # Build: Install dependencies with Bun (respecting workspaces/lockfile), then build
    buildCommand: (curl -fsSL https://bun.sh/install | bash) && ~/.bun/bin/bun install
    # Start: Run with the native bun
    startCommand: cd apps/backend && bun run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        sync: false
      # Secrets managed in Dashboard (sync: false prevents overwrite)
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
      - key: REDIS_URL
        sync: false
      - key: BACKEND_URL
        value: https://vext-backend-yj77.onrender.com


  # --------------------------------------------------------------------------------
  # GAME SERVER (Dedicated Aether Strike) - apps/game-server
  # --------------------------------------------------------------------------------
  - type: web
    name: vext-game-server
    region: frankfurt
    env: rust
    plan: free
    rootDir: .
    buildCommand: cd apps/game-server && cargo build --release
    startCommand: cd apps/game-server && ./target/release/game-server
    envVars:
      - key: NODE_ENV
        value: production
      - key: BACKEND_URL
        value: https://vext-backend-yj77.onrender.com/api
      - key: PORT
        sync: false
      - key: JWT_SECRET
        sync: false
