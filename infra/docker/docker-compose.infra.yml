services:
  mongo:
    image: mongo:latest
    container_name: vext_mongo
    command: [ "--replSet", "rs0", "--bind_ip_all", "--port", "27017" ]
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - vext-net

  redis:
    image: redis:alpine
    container_name: vext_redis
    ports:
      - "6379:6379"
    networks:
      - vext-net

  postgres:
    image: postgres:16
    container_name: vext_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-vext_finance}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - vext-net

  server:
    build:
      context: ../..
      dockerfile: apps/server/Dockerfile
    container_name: vext_server
    ports:
      - "3002:3002"
    env_file:
      - ../../.env
    environment:
      - MONGODB_URI=mongodb://mongo:27017/vext
      - REDIS_URL=redis://redis:6379
      - POSTGRES_HOST=${POSTGRES_HOST:-postgres}
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - POSTGRES_DB=${POSTGRES_DB:-vext_finance}
      - WS_PORT=3002
      - SERVER_ROLE=launcher
    depends_on:
      - mongo
      - redis
      - postgres
    networks:
      - vext-net

  games-server:
    build:
      context: ../..
      dockerfile: apps/server/Dockerfile
    container_name: vext_games_server
    ports:
      - "3003:3003"
    env_file:
      - ../../.env
    environment:
      - MONGODB_URI=mongodb://mongo:27017/vext
      - REDIS_URL=redis://redis:6379
      - POSTGRES_HOST=${POSTGRES_HOST:-postgres}
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - POSTGRES_DB=${POSTGRES_DB:-vext_finance}
      - WS_PORT=3003
      - SERVER_ROLE=games
    depends_on:
      - mongo
      - redis
      - postgres
    networks:
      - vext-net

networks:
  vext-net:
    name: vext-net
    driver: bridge

volumes:
  mongo_data:
  postgres_data:
